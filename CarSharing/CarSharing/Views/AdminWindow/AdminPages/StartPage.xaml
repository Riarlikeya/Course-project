<Page x:Class="CarSharing.Views.AdminWindow.AdminPages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarSharing.Views.AdminWindow.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.ColumnSpan="3" Grid.RowSpan="3" BorderBrush="Transparent" BorderThickness="0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
               <TextBlock x:Name="UserName" Text="{Binding UserName}" FontSize="24"/>
               <TextBlock x:Name="TotalCarsAmount" Text="{Binding TotalCarsAmount}" FontSize="16"/>
               <TextBlock x:Name="CurrentDateTime" Text="{Binding CurrentDateTime}" FontSize="16"/>
            
            </StackPanel>
        </Border>
    </Grid>
</Page>
